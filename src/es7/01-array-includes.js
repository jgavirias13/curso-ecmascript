// Permite saber si hay un elemento especifico en un array

const numbers = [1, 2, 3, 4, 5, 6];

if (numbers.includes(7)) {
  console.log('Contiene el 7');
} else {
  console.log('No contiene el 7');
}

if (numbers.includes(4)) {
  console.log('Contiene el 4');
}