const pet = {
  name: 'Balto',
  age: 0.3,
  breed: 'Siberian Husky'
}

console.log(Object.values(pet)); //Obtener los valores de las propiedades del objeto
console.log(Object.keys(pet)); //Otener los nombres de las propiedades del objeto