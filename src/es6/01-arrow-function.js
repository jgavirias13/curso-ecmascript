function square(number1) { //Funcion en bloque
  return number1 * number1;
}

const arrowSquare = (num) => { //Arror function
  return num * num;
}

const arrowSquare2 = num => num * num; //Funcion arrow function inline (sin return) - codigo simplificado
